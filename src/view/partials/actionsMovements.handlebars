    {{#if showActions}}
    <td><a href="/movements/details/{{movement_id}}">Visualizar</a></td>
        {{#if isComplete}}
            <td></td>
        {{else}}
            <td><a href="/movements/entry/{{movement_id}}" class="confirmationButton">Dar entrada</a></td>
        {{/if}}
    {{else}}
    <td></td>
    {{/if}}
    <td></td>