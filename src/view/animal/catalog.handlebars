    <ul class="animal_container float">
            {{#each catalog}}
            <li class="float-item">
                    <a href="/animals/details/{{_id}}">
                        {{#ifGenderCond gender}}
                            <img class="animal-image-male" src="{{photoLink}}" alt="Fotografia do animal"/>
                        {{else}}
                            <img class="animal-image-female" src="{{photoLink}}" alt="Fotografia do animal"/>
                        {{/ifGenderCond}}
                        <p></p>
                        <label class="animal-list-name">{{name}}</label>
                    </a>
            </li>
            {{/each}}
    </ul>